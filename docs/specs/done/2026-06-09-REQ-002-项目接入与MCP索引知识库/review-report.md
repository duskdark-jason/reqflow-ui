# 项目接入与 MCP 索引知识库前端 Review 报告

## Review 结论

- 结论：有条件通过
- Review Agent：Codex Review Agent
- Review 时间：2026-06-09

## 审查输入

- `requirement.md`
- `plan.md`
- `execution-report.md`
- 代码 diff
- 静态代码审查输出

## 问题清单

以下问题清单保留首次 Review 发现；最终处理状态以“复审记录”和“最终结论”为准。

| 严重级别 | 文件 | 问题 | 风险 | 建议 |
|---|---|---|---|---|
| 阻断 | `src/views/requirement/demand/index.vue:596`、后端 `ReqImpactItemMapper.xml:51` | 前端已把 `variantId` 传给影响面推荐接口，但后端当前未使用客户线/基线分支过滤，也不限定最新索引批次。 | 需求表单展示的推荐结果可能来自其他地区客户线或旧 commit，AC-UI-003、AC-UI-004 不能验收。 | 等后端 RF-002 调整契约后同步前端查询字段和展示口径，确保客户线选择会真实影响推荐结果。 |
| 重要 | `src/views/requirement/project/index.vue:124`、`src/views/requirement/project/detail.vue:25` | “新增项目”弹窗仍只能维护项目基础字段；前后端仓库和客户基线只能在分散菜单中维护，项目接入中心也只是查看与 MCP 指引，没有在项目初始化流程内提供统一维护入口。 | 与用户预期的“项目管理菜单内新增项目即可填写项目名称、前后端仓库、统一分支/客户基线”仍有差距，管理员初始化项目时会回到分散 CRUD。 | 增强项目新增/接入中心：至少在接入中心提供仓库和客户基线的新增/编辑入口，或做分步初始化弹窗；平台只保存共享 Git 信息和基线分支，不保存个人本机目录。 |
| 重要 | `docs/specs/active/2026-06-09-REQ-002-项目接入与MCP索引知识库/execution-report.md:31` | L3 页面冒烟和 L4 跨端联调未执行。 | 新增动态路由、项目接入中心、需求弹窗推荐和权限按钮没有运行态证据，不能进入完成态。 | 返修后启动前端并打开项目接入中心、需求新增/编辑弹窗、Agent 交接资料；后端可用时补跨端联调。 |

## 验收 ID 覆盖矩阵

| 验收 ID | 需求描述 | 实现证据 | 验证证据 | Review 结论 |
|---|---|---|---|---|
| AC-UI-001 | 项目接入中心查看项目、仓库、客户基线、索引状态和模块知识库 | `project/detail.vue`、`project/index.vue` | 构建记录通过 | 有条件通过，初始化维护入口不足 |
| AC-UI-002 | 提供 MCP 索引推送指引并说明不保存本机绝对路径 | `project/detail.vue` MCP 指引 | 构建记录通过 | 通过 |
| AC-UI-003 | 选择项目、客户线和模块后加载推荐影响面 | `demand/index.vue` 调用 `suggestImpact` | 构建记录通过 | 失败，后端未按客户线/最新批次返回可信推荐 |
| AC-UI-004 | 推荐影响面可追加且保留人工编辑 | `appendImpactSuggest` 追加文本字段 | 构建记录通过 | 有条件通过，依赖 AC-UI-003 推荐准确性 |
| AC-UI-005 | Agent 交接资料入口可进入现有 artifact 页面 | `demand/detail.vue`、`package/index.vue` | 构建记录通过 | 通过 |
| AC-UI-006 | 前端 harness 文档同步 | UI 契约和模块文档已更新 | 执行报告记录 L0 通过 | 通过 |

## 验收复核

- 项目接入中心展示：有条件通过，查看能力存在但初始化维护动作不足。
- MCP 索引指引：通过。
- 需求影响面推荐：失败，后端未按客户线/最新批次收敛。
- Agent 交接资料语义调整：通过。
- 运行态页面证据：未验证。

## 返修交接清单

| 修复 ID | 严重级别 | 关联验收 ID | 问题 | 修复要求 | 验证要求 |
|---|---|---|---|---|---|
| RF-001 | 阻断 | AC-UI-003, AC-UI-004 | 推荐结果未按客户线/基线和最新批次可信过滤。 | 同步后端 RF-002 后，调整前端推荐查询和展示；如果新增 branch/batch 字段，页面必须传递并展示当前推荐来源。 | 运行 `npm run build:prod`；跨端验证切换客户线后推荐结果变化且不混入旧批次。 |
| RF-002 | 重要 | AC-UI-001, AC-UI-002 | 项目新增/接入中心未提供前后端仓库与客户基线统一维护流程。 | 在项目接入中心或新增项目流程内提供仓库、客户基线新增/编辑入口；明确只保存共享 Git 远端和基线分支，不保存个人本机路径。 | 运行 `npm run build:prod`；L3 页面冒烟覆盖项目初始化、仓库/客户线维护入口和 MCP 指引。 |
| RF-003 | 重要 | AC-UI-001, AC-UI-003, AC-UI-005 | 缺少运行态页面和跨端联调证据。 | 返修后补充 L3 页面冒烟记录；后端可用时补 L4 影响面推荐和需求保存联调。 | 启动前端，检查项目接入中心、需求弹窗、Agent 交接资料页面和 console/network；更新 `execution-report.md`。 |

## 复审记录

| 修复 ID | 执行处理结果 | 复审结论 | 复审证据 |
|---|---|---|---|
| RF-001 | 已修复：前端推荐请求字段与后端客户基线/最新批次契约一致，后端已消费 `variantId`。 | 通过 | `npm run build:prod` 通过；后端 `mvn -pl ruoyi-requirement -am test` 13 个测试通过 |
| RF-002 | 已修复：项目接入中心已提供仓库和客户基线新增/编辑入口，并明确不保存本机目录。 | 通过 | `src/views/requirement/project/detail.vue`；`npm run build:prod` 通过 |
| RF-003 | 已处理：前端 dev server 在 18081 启动成功，浏览器进入登录页，console 0 error/0 warning，`captchaImage` 请求 200。 | 有条件通过 | 当前执行 agent 未持有登录态账号/token，登录后页面业务操作和跨端保存需补验 |

## 最终结论

有条件通过。阻断项 RF-001 和维护入口 RF-002 已修复并通过构建；RF-003 已补充前端运行态冒烟。剩余条件是登录态下的项目接入中心维护、影响面推荐和需求保存需在具备测试账号/token 的环境继续补验。

## 关闭备注

用户已决定结束当前版本。当前版本按 MCP 索引和项目接入中心基础能力归档；后台项目管理菜单完整初始化体验未达到最初设想，应作为下一版独立需求处理。

## Review 分级说明

- 阻断：需求不可用、数据错误、安全/权限风险、迁移风险、缺少关键验收证据、无证据环境结论。
- 重要：契约不一致、关键测试缺口、计划承诺的运行态或增强验证未完成且影响真实流程。
- 一般：命名、文档清晰度、低风险维护建议。
