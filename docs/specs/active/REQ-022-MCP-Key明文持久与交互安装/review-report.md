# MCP Key明文持久与交互安装Review报告

## Review 结论

- 结论：通过
- Review 类型：本地自检
- 范围：MCP 管理页展示、统一安装命令渲染、管理员请求地址配置、需求入口互斥、harness 文档

## 检查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 字段隐藏 | 通过 | 覆盖 AC-001、AC-002。列表和弹窗不再单独展示明文 Key 或 Key 前缀。 |
| 命令渲染 | 通过 | 覆盖 AC-004、AC-007。`plainKey` 保留在状态中，只用于替换安装命令占位符，并兼容 `key.plainKey` 以支持下次打开。 |
| 统一展示 | 通过 | 覆盖 AC-003。普通弹窗仍只展示顶层统一安装命令，不按客户端分组。 |
| 管理员配置 | 通过 | 覆盖 AC-008。MCP 请求地址配置入口只在管理员角色展示，点击后用弹窗调用专用配置接口。 |
| 重新打开按钮 | 通过 | 覆盖 AC-009。页面不再展示或保留重新打开安装命令按钮，复看命令走行内“使用指令”。 |
| 需求入口互斥 | 通过 | 覆盖 AC-010。需求列表页不再同时展示同阶段生成指令和反馈结论入口；详情页按 `requirement_assessment` 或 `requirement` 是否已回写切换生成指令和反馈结论。 |
| 文档同步 | 通过 | 覆盖 AC-005、AC-007、AC-008、AC-009、AC-010。模块文档、UI 契约和搜索索引已同步。 |
| Harness 门禁 | 通过 | 覆盖 AC-006。`--spec` 指向 `docs/specs/done/` 会失败，当前执行 spec 保持在 `active/`。 |

## 风险说明

- 需求列表页没有资料包版本明细，因此无法判断需求分析或设计产物是否已回写；列表页优先避免双入口，反馈结论在详情页按资料包状态展示。

## RF项

无。
