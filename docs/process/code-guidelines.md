# 代码规范

本文件用于沉淀项目实现细则。`AGENTS.md` 只保留入口、红线和验证要求；具体语言、框架、组件、数据库、接口、日志和样式规则写在这里。

## 使用方式

- 按项目技术栈改写本文件，不保留无关框架规则。
- 规则要基于当前项目事实，不写“可能使用”的泛化判断。
- 高风险规则应写清触发场景、必须检查的文件和验证方式。
- 如果规则只适用于某个业务模块，应优先沉淀到 `docs/ai-harness/modules/` 或 `docs/domains/`。

## 建议章节

- 语言和框架规则。
- 数据访问或 API 调用规则。
- 组件、页面或样式规则。
- 权限、安全和日志规则。
- 常见错误和必须复用的本地工具。
